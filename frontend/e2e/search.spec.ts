import { test } from '@playwright/test';

test.describe('Search', () => {
  test.skip('search input is present (skipped - search removed)', async () => {
    // Search is removed from the frontend homepage
  });

  test.skip('typing in search filters songs (skipped - search removed)', async () => {
    // Search is removed from the frontend homepage
  });
});
